# Production API URL — Railway backend (1.1M points, Node.js + PostGIS)
# CRA reads this at build time via REACT_APP_ prefix convention.
# Safe to commit — this is a public URL, not a secret.
REACT_APP_API_URL=https://samaajdata-backend-production.up.railway.app
